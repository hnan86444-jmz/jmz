FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY servers/ /app/servers/
COPY agents/ /app/agents/
COPY mcp-config.json /app/

EXPOSE 8000

CMD ["python", "-m", "agents.mining_agent"]